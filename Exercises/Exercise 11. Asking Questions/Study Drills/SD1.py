# Python input () is a built-in function that reads
# a line from input, converts it to a string
# (stripping a trailing newline), and returns
# that. Input can be from different sources but
# most often the input is from the keyboard.