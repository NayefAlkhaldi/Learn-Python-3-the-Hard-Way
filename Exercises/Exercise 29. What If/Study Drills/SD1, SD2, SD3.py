# I think it makes something when it's True. Like:
if True == True:
    print("True == True")

# it needs four spaces to tell python that this part of the code
# follows the exception.
# If it isn't indented then an Indentation error will be raised.