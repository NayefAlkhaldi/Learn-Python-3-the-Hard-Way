# The number to do it in different ways is 362880!!!
# Let's try some of them:
# print(multiply(age, add(height, divide(weight, subtract(iq, 2)))))
# print(add(iq, multiply(height, subtract(weight, divide(age, 2)))))
# hmmm...

# Done!